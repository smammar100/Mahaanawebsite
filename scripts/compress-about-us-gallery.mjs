/**
 * Archived one-off: previously compressed "About us" source images to `about-us-*.webp`.
 * Source files and outputs were removed from the repo as unused. Restore sources under
 * `public/images/invest/` if you need to run this again, then uncomment the logic below.
 */
console.warn(
  "[compress-about-us-gallery] Skipped: gallery source WebP pipeline assets were removed from the repo."
);
process.exit(0);
