# Sanity (get token from https://www.sanity.io/manage → API → Tokens)
NEXT_PUBLIC_SANITY_PROJECT_ID=ke6ev8q3
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_WRITE_TOKEN=
# For import scripts (e.g. import-roshan-post): add to .env.local without overwriting
SANITY_WRITE_TOKEN=
# Deploy token for schema deploy (npm run schema:deploy); use in CI as secret
SANITY_AUTH_TOKEN=
